import { CriterionToStringPipe } from './criterion-to-string-pipe';

describe('CriterionToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new CriterionToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
